{
  "compilerOptions": {
    // 因为tsconfig支持JSONC，所以比package好用一万倍！
    // 没有注释的JSON不配做配置文件！
    // this project only work on > NODE.JS V20 LTS version.
    // mainly support GitHub Action operate.
    "target": "es2023",
    "module": "node16", // not exist node20 LTS version yet.
    // FETCH: >= node18
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true
  },
  "files": [
    "index.mts"
  ]
}
